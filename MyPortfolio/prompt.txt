Prompt

You are a Senior ASP.NET Core MVC Engineer, Frontend Architect, and CSS Debugging Expert.

Repository:
https://github.com/MohamedSaber2004/MyPortfolio

Objective

Before redesigning the UI, perform a complete frontend audit to identify the root cause of why the authentication pages (Login and Register) have broken layouts even though the CSS files appear to be loading.

Do not apply temporary CSS fixes. Find and fix the underlying issue.

Phase 1 – Root Cause Analysis

Analyze the entire authentication system, including:

Layouts
_Layout.cshtml
_AuthLayout.cshtml (if present)
Shared layouts
Partial views

Verify that the correct layout is used by Login and Register.

CSS Loading

Inspect every stylesheet loaded by the authentication pages.

Verify:

Bootstrap CSS
Site CSS
Authentication CSS
Theme CSS
Vendor CSS

Check:

Loading order
Duplicate imports
Missing files
Incorrect paths
Conflicting styles
Grid System

Inspect whether Bootstrap's grid system is functioning correctly.

Check whether:

.container
.container-fluid
.row
.col-*

have been overridden.

If Bootstrap classes have been redefined, locate where and explain why.

Authentication Wrapper

Inspect the authentication wrapper.

Determine whether the intended layout should use:

Flexbox
CSS Grid
Bootstrap Grid

Verify that the wrapper correctly applies:

display:flex
display:grid
justify-content
align-items
gap
max-width
margin:auto

If not, explain why.

CSS Conflicts

Search the entire project for CSS rules affecting:

container
row
col
card
form
form-control
input
body
main
section
.auth
.login
.register

Identify any CSS rules overriding Bootstrap or the authentication styles.

JavaScript

Check whether any JavaScript manipulates authentication layout or classes after page load.

Browser Rendering

Determine why the page renders as:

Brand Section

↓

Login Form

instead of

Brand Section | Login Form

Provide the exact reason.

Phase 2 – Fix Root Cause

After identifying the issue:

Fix it properly.

Do NOT use:

!important
Fixed margins
Fixed widths
Random padding
CSS hacks

Instead, restore the intended layout architecture.

Phase 3 – Redesign Authentication

Once the root cause is resolved, redesign Login and Register pages.

Requirements:

Centered authentication card
Max width around 1100px
Two-column desktop layout
Single-column mobile layout
Premium SaaS appearance
Proper spacing system (8px scale)
Rounded cards
Soft shadows
Glassmorphism (optional)
Professional typography
Responsive design
Smooth animations
Consistent buttons
Modern inputs
Google login styling
Password toggle
Loading states
Toast notifications
Accessible design

The authentication pages should resemble those of GitHub, Clerk, Vercel, Stripe, or Linear.

Phase 4 – Repository-wide UI Audit

After fixing authentication, analyze every Razor View in the repository.

Check for:

Broken spacing
Margin inconsistencies
Padding inconsistencies
Nested containers
Duplicate CSS
Inline styles
Bootstrap misuse
Responsive issues
Poor typography
Inconsistent cards
Poor button styles
Broken grids

Create a reusable design system and refactor all views to follow it.

Deliverables

Provide:

The exact root cause of the authentication layout issue.
Every file causing the issue.
Every modified file.
The reason for each modification.
A summary of CSS conflicts found.
Bootstrap issues found.
Layout issues found.
Responsive improvements.
Performance improvements.
A final UI architecture summary.

Important: Do not start redesigning until the root cause has been identified and fixed. Avoid cosmetic patches—implement a clean, maintainable solution that follows ASP.NET Core MVC and modern frontend best practices.